---
description: Continue a claude-control goal
argument-hint: Optional goal slug or instruction
model: sonnet
color: yellow
---

Use `ccctl goals --help` to discover the installed CLI. If $ARGUMENTS names a goal, show and work on that goal. Otherwise use `ccctl goals next --json`, then start it and iterate until done, blocked, or waiting on PR/human review.
